{
  "name": "http-functions",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "postinstall": "lerna bootstrap --hoist --no-ci",
    "release": "lerna run release"
  },
  "devDependencies": {
    "lerna": "^3.3.2"
  }
}
